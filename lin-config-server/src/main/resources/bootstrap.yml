server:
  port: 9000
spring:
  application:
    name: lin-config-server
  profiles:
    active: remote

#设置暴露actuator的那些接口，*为暴露全部接口
management:
  endpoints:
    web:
      exposure:
        include: '*'